# dependency_example

A demo package with no purpose of its own: it's a dependency of
`subject_example` (a real dependency edge) and a second subject for the demo to
attest about. It only needs to be a published package with a stable id.
