# subject_example

A demo package — the "subject" a viewer browses in the attestation demo, whose
attestations a consumer inspects.

The one notable thing about it: it **depends on `dependency_example`**. That's a
real dependency edge (it shows up in MVR's Dependencies tab), so the demo can
show attestations on both a package and one of its dependencies.
